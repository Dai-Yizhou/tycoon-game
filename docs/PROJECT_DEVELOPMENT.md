# 大富翁.io · 项目开发报告

> 生成日期：2026-09-25 · 主线依据：dev-Dai（266 提交）+ git churn 实测 + 工作区外历史原型目录。
> 本报告合并自原 `DEVELOPMENT_TRAJECTORY.md` 与 `ENGINEERING_MATURITY.md`。

## 0. 一句话概括

从 2026-04/05 一份"连续运转、大棋盘、无回合"的 `.io` 式大富翁创意出发，先以最小 JS 原型验证核心闭环，再迁入 TypeScript monorepo，历经"服务端权威化 → 系统性技术债清理 → 客户端状态架构重塑 → 主题/配置数据驱动 → 玩法系统逐块落地"，逐步收敛为一个数据驱动、服务端权威、面向内测的可持续项目。

---

## 一、演进脉络

### 1. 立项背景（概念期，2026-04 ~ 05）
- 创意记录于旧原型 README（署名 20260516）：堂弟大富翁没人陪玩、纸质不方便，受 `florr.io` 启发。
- 核心理念：**无回合**、棋盘不间断运转、随时加入/退出、破产再来；大棋盘所有玩家共处大世界；"以理解建立优势，不以时间金钱"。
- 期初方案 `项目基本方案.txt` 已规划 8 类格子（地产/空格/事件/投资/交通枢纽/纪念碑/起点/监狱）+ 财产（破产/抵押/合租）、信用+银行、昼夜计税等超前设计。

### 2. 原型阶段（JS，2026-05 ~ 07）
- 目录 `old/tycoon-game(old)/`：`server.js`（336 行单文件 Express+Socket.IO）+ `public/game.js`（376 行 Canvas 客户端）+ `behaviors/*.js`（171 行）。
- 里程碑：单文件内实现掷骰冷却/买地/升级/聊天/破产；首创**行为脚本化**与**连续掷骰+冷却**——这两个内核沿用至今。
- 码量：~940 行 JS + ~40 行 JSON。验证"核心闭环可行"，但逻辑全耦合在连接回调里，催生 TS 重构。

### 3. 早期 TypeScript 迁移（2026-07-21 ~ 08-04）
- 首批提交（`1d37ab0 Initial commit`、`f8b03d0 Merge main`），`tycoon-game-githubclone/` 即此快照（7 提交）。
- 转向**服务端权威化**：`2da8ed6` 岔路/迷你地图/骰子配置、`1ef8ccd→e3b58e8` 组队权威化、`2075174` 客户端模块化+i18n。

### 4. UI 重构与架构清理（2026-08-04 ~ 08-14）
- `ui-Dai` 分支 HUD 重构 08-12 合并回主工作区（`4ae8423`）。
- 系统性架构清理（`fix/complete-authority-hardening`）：`52089ae` 集中 socket 注册、`0bdfb6b` 单一权威事件管线、`f1f8716` 移除旧 DOM 管线。
- 08-14 大步删过时系统：`0bc37e3` 道具/天赋/成就第一批、`f01b2d9` 银行借贷、`edbd240` 抵押竞拍、`8ae396a` 组队改非经济层、`0043b70` 复活制改破产重开、`596a6ce` 移除 admin 包。

### 5. 权威加固完成（2026-08-15 ~ 08-18）
- `f8bab0e` 原子破产清算+现实时间监狱冷却、`5cd9951` 世界快照持久化、`1f0b65b` 显式 Socket 认证与限流、`04687de` JWT 握手闭环、`60fe747` 接入 Mongo 账号。
- 服务端权威 + 认证 + 持久化三件套成型。

### 6. 客户端状态架构重塑（2026-08-19 ~ 08-21）
- 明确"单一状态投影"：`b684a9c` 单一状态边界、`bb135e6` 移动状态进 store、viewModel 即 store 投影。
- 清理旧渲染层：`d6adcc8` 干掉 renderer 与 utils（Canvas 渲染被取代）。

### 7. 主题化 / 区域化（2026-08-20 ~ 08-23）
- `3e24060` 清理主题硬编码 → `059aab2` 全区域令牌化 → `7735f0f` 按格子配置 theme/region/timezone。
- 沉淀 design token 与全套 UI 视觉方向文档。

### 8. 行为系统迁移 + 存储 + 地图拆分（2026-08-24 ~ 08-26）
- 行为系统迁移、状态从运行时存储（`d38da4e`）到接入 Mongo（`e6190e1→e22d3ee`）。
- 地图数据拆出独立 `map-repo`（`d468d75`），主仓以 `map.json/map-meta.json` 数据驱动。账号系统 beta 边界闭环（`44e901d`）。

### 9. 玩法系统逐块落地（2026-08-27 ~ 09-05）
- `ff6fb3a` 聊天框指令+组队闭环；`4aa277f` 区域数值全面 **UCT 驱动**；`6169475` 架构健康度修复；榜单 `c7b5876`、成就 `11736b5`；`267c417` 加固 chat 与经济结算。

### 10. 经济配置化与信息边界（2026-09-06 ~ 09-12）
- `22393db` 经济事件配置化链路；`1e6f144` cell-hover/act-bar 信息边界 + 静态价格购买；`5cc9f39` 内测前清理 ServerConfig 字段。

### 11. 视听收尾与收敛（2026-09-13 ~ 09-14)
- 掷骰按钮冷却揭示、全屏转场视效、动画参数令牌化；含 revert（撤销事件 toast/时区拆分），收敛内测前过度设计。

### 12. 权威收敛 + 内测口径落定（2026-09-17 ~ 09-25）
- 区域时钟权威化：`b94ba67` dayRatio 权威下发，客户端 isDay 边界改读服务端值；`d9e12dc` 按目标格时区结算消除预览/实付漂移；`1e6f6b1`/`7fec1e0` 统一相位口径。
- 数值与状态单一来源收敛：`518cdd2` publishValueChanged 单一发射点、`c673ca7` GameStore 单源（删注册默认实例），消除"掷骰后卡死/区域 pros 漂移"等状态竞争问题。
- 重连与移动插值修复：`3fe9670` 重发 login 重建 otherPlayers、`a427a58` 重连广播 playerJoined、`4ba655d` 其他玩家移动权威插值。
- i18n 与设计令牌：`7f6f86c` 系统消息走 i18n、`3729dcb` 落盘四地域新令牌、`185c0cd` 回退主题参数。
- 内测口径落定：`973ed1b` 计税/收租/昼夜/投资广播系统消息、`7d3814b` 破产触发字段可观测、`dfb55b1` 已存 token 水合转正身份、`7b4320b` 启动脚本强制 MongoDB、`14ad644` 世界快照跨进程恢复测试 + 落定内测账号/破产口径。

---

## 二、工程体量对照

### 硬指标（当前主线快照）
| 指标 | 数值 |
|------|------|
| 提交数 | 266 |
| 净 TS 代码 | 32,438 行（src 22,128 / test 10,276） |
| 累计代码 churn | 新增 85,092 行 / 删除 51,301 行 |
| 包拆分 | shared 4.2k / server 18.7k / client 9.6k |
| 测试套件 | 服务端 51 / 客户端 27 / shared 13，共 91 |

### 同类项目对照
| 参照系 | 规模特征 | 本作定位 |
|--------|----------|----------|
| 浏览器 `.io` 小游戏（agar/florr 早期核心） | 核心逻辑 1–5 万行 | ≈ 同级 |
| 开源网页大富翁 | 1–3 万行 | 明显超越 |
| 商业独立棋盘游戏 | 5–15 万行（含工具链/美术管线） | 仍偏小（缺资产管线） |
| 商业 MMORPG/多人大世界服务端 | 数十万行 + 海量资产 | 差一个数量级 |

**结论**：**中型浏览器实时多人小游戏**体量，高于 florr.io 早期与普通网页大富翁，但资产/内容管线仍缺失，呈"服务端重、资产轻"的配比。近一周净码量与测试覆盖明显补齐，工程侧更接近正式内测形态。

---

## 三、技术决策：常见 vs 创新

### 常见（行业标准，占绝大多数）
- Socket.IO 实时 + 服务端权威状态（MMO 防作弊教科书模式）。
- Express + MongoDB + JWT 账号；客户端"单一状态投影"；数据驱动地图/行为；i18n + 主题令牌化 + 昼夜/时区驱动。

### 创新 / 少见
- **玩法层是最大创新**：无回合、连续掷骰+冷却、棋盘不间断运转、随时进出、破产再来，改写大富翁回合制底层。
- 行为脚本化 + 数据驱动时代封印；UCT 数值表外置驱动区域数值；无回合下的服务端权威+全局广播组合。

**判断**：**"常见工程 × 创新玩法"** 是典型独立游戏形态。工程决策全走成熟稳妥路径，差异化押在玩法规则上——稳健且分工健康。

---

## 四、薄弱板块与投入边界（结合当前工作概况）

> 工作概况：**近期准备发布内测；单人 + agent 推进开发；缺少美术/音效专业人才；地图内容独立于开发进程。**

**总原则：内测前只投入"会直接导致发布失败 / 无法回滚 / 主流程崩溃"的项（工程化、关键 E2E）；其余以占位、锁特性、独立进程应对，不扩大投入。**

| 板块 | 是否加强投入 | 投入边界 |
|------|:---:|------|
| **美术 / 音效** | **否** | 缺专业人才，短期不投。边界：用 主题令牌 / SVG 占位顶到内测；开放主题配置使非美术可低成本替换；不引入第三方资产管线；不阻塞内测。    |
| **性能 / 多人扩展** | **否（先测后投）** | 内测规模小，未出现真实瓶颈前不投入。边界：仅做低成本广播节流；不建 shard/分区/防重。等内测出现瓶颈再投入。 |
| **运营 / 工程化** | **是（唯一内测前加强项）** | 直接关系能否发布与回滚。边界：最小可行 = 一键打包产物 + 单机部署脚本 + 错误日志落盘；不做完整 CD 流水线 / 监控大屏 / 灰度。 |
| **测试覆盖** | **选择性** | 只补主流程 E2E（掷骰 / 买地 / 破产 / 组队闭环），不做全量覆盖。 |
| **玩法 / 数值** | **收敛不新增** | 锁定内测特性集；经济/成就只做数值调参，不加新系统；不再反复重构。 |
| **地图内容** | **否（独立进程）** | 不占开发人力。边界：开发仅保证读取链路兼容新地图格式，内容制作由独立进程负责。 |
| **AI 玩家** | **否（非内测前提）** | 不作为发布前置；如需陪玩/压测再轻量接入。 |

### 投入优先级总结
1. **必须（内测前）**：工程化最小可行（打包/部署/日志）+ 主流程 E2E。
2. **可缓（内测后）**：美术/音效（等人才或占位演进）、性能扩展（按瓶颈触发）、玩法深度。
3. **保持独立**：地图内容、AI 玩家。

---

## 五、工作量统计

### 口径说明
churn 实测累计新增 **85.1k 行**（含后来删除的重写），最贴近实际投入；净 33.8k 反映当前规模。

### 估算（含设计/测试/文档/重构）
| 口径 | 行数 | 产出密度 | 人日 | 人月* |
|------|------|----------|------|-------|
| 净产出（现状） | ~33.8k | 200 行/人日 | ≈170 | ≈8 |
| 累计投入（含重写） | ~85.1k | 150 行/人日 | ≈567 | ≈26 |
| 修正后（含协调/返工系数 0.7） | — | — | ≈400–567 | ≈18–26 |

\* 1 人月 ≈ 22 人日。

### 自然时间核对
Git 跨度约 2.5 个自然月。若单人全职，567 人日 / 56 天 ≈ 10 人日/天，超单人极限 ⇒ 多人协作 + 兼职/探索性低产出密度，或叠加重写返工。提交类型（fix 74 / feat 61 / refactor 33 / docs 21 / revert 2，累计删除 5.1 万行）显示**约四成工作量投入修复与重构**，其中 fix 已反超 feat，反映从"加功能"转向"保稳定"——符合内测前收敛特征。

### 结论
**主观体量偏轻（8 人月级），实际累计投入（18–26 人月）高出一倍以上**，差异来自高频重构、revert 与跨分支合并。控制返工、补齐 CI 与测试后可显著降低单位特性成本。

---

## 六、演进主线的"不变内核"

1. **无回合连续掷骰 + 冷却**：从 JS 原型贯穿至今，始终是玩法骨架。
2. **格子行为可配置**（behaviors → BehaviorEngine → 数据驱动 behavior JSON）：起源最早，延续最久。
3. **服务端为唯一权威**：反复加固，客户端仅投影状态 + 发请求。
4. **数据/配置驱动**：board.json → map-repo → UCT 数值表、经济/主题/时区全部外部化。
5. **"走完先收敛"**：revert/合并/删除高频，保持主线干净、不欠设计债。

## 七、技术债 / 待观察

- 同一特性"实现→部分 revert"反复（toast、时区拆分、动效），最新已收敛。
- 道具/天赋/成就曾全部移除又逐个回归（成就已回归），item/talent 仍"未实现"，规划重于实装。
- AI 玩家（ai-bot）因主仓清理被移出，当前 `shared-repo` 内已无。
- **破产口径（已确认，已补可观测性）**：money/credit 的 `min` 为 0（`packages/server/map-meta.json`），`EconomyService.changeValue`（`packages/server/src/economy/EconomyService.ts:34`）把所有扣减钳制到 min 且不因余额不足报错，因此玩家在任何经济操作下都无法负债——口径定为「任意玩家数值 ≤ min 即破产」。触发点 `Bankruptcy.onPlayerUpdated` 逐字段判定（`previous > min && current <= min` 才计入，起始值本就等于 min 不触发），记录触底明细（fieldId/fieldName/previous/current/min）后广播系统消息，并在 `server.playerBankrupt` 载荷携带 `triggeredFields`，客户端破产页据此展示「触发原因」专属提示。覆盖：`packages/server/tests/bankAuthority.test.ts`、`packages/client/tests/pages.test.ts`。
- **内测账号口径（已确认）**：账号与设备一一绑定，转正仅输入新的用户名、不设密码，接受换设备/清缓存即丢失。因此 `migrateGuest`（`packages/server/src/auth/AuthService.ts:309-318`）设 `passwordHash = null` 导致的「转正后无法再登录」不作为缺陷处理。转正瞬间 socket 仍持旧 token（世界内 `player.username` 仍为 `guest_xxx`，以该前缀判定游客身份处仍按游客对待，须等一次重连刷新）；客户端已改为从已保存 token 的载荷水合身份（`packages/client/src/auth/authApi.ts:decodeTokenUser`、`AuthSession`），重启后仍能区分游客/正式账号并保留转正入口。
- **跨进程保持依赖显式存档名**：账号与局内进度都需跨进程保持。账号落在 Mongo（`users`），局内进度落在 Mongo `world_snapshots`（键为 `WORLD_ID` + `WORLD_NAMESPACE`）。未设 `WORLD_ID` 时每次启动生成新的临时存档（`temp_*`，按 TTL 清理），适合调试用新档；要续档须显式 `WORLD_ID=<存档名> pnpm dev`。启动脚本（`scripts/dev-services.mjs`）强制注入 `MONGO_URI`，文件/内存实现已弃用。跨进程恢复链路由 `packages/server/tests/storage/MongoWorldStore.test.ts` 覆盖。
- **`Frozen` 状态口径不一致**：地产购买/升级入口显式拒绝 `Frozen`（`packages/server/src/handlers/propertyHandler.ts:198`），而经济资格体系 `participatesInEconomy` 认为 `Frozen` 可参与（离线照常计税与收租）。是刻意分层还是遗漏待确认。
