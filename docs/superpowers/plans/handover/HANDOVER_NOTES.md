这份文档由:Dai于26/7/12手写。

配置的时候注意目录位置和外部应用的导出位置。依赖项不小（～160MB），我运行时是在2个硬盘上反复横跳的

AI玩家应用是个巨坑，我调试只用了比较小的本地模型，较早版本里文字LLM可以正常输出评价，但后来接了（实际上并没接上）视觉LLM就全炸了。没有充分验证LLM实际参与了AI玩家决策。本来想着让它找bug，结果自己炸了一堆bug...

目前的UI还能将就看看（拿去内测应该问题不大），trae看不懂设计手稿，所以我用了florr wiki的资料做的调整。

在客户端上只验证了部分单机逻辑，还没验证（在spec中出现但未知是否完整实现并调用/未知能否正确工作）的有：地产收租与合资、天赋启用、组队数值共享、时代结算与切换
（应该还有不少，但接口都留了 ('';)

服务端千人并发没有测过，因为我的电脑肯定扛不住（因为断网、炸内存、炸内置硬盘，trae的进程挂了有几十次 :(



